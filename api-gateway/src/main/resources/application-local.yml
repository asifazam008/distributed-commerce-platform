server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:

      # ================= DISCOVERY LOCATOR =================
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      # ================= HTTP CLIENT CONFIG =================
      httpclient:
        response-timeout: 30s

      # ================= DEFAULT FILTERS =================
      default-filters:
        - RemoveResponseHeader=Transfer-Encoding

      # ================= STATIC ROUTES =================
      routes:

        # -------- AUTH SERVICE --------
        - id: auth-service
          uri: lb://authentication-service
          predicates:
            - Path=/auth/**

        # -------- ORDER SERVICE --------
        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/orders/**

        # -------- USER SERVICE --------
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/users/**

# ================= JWT CONFIG =================
jwt:
  secret: MyVeryStrongAndSuperLongSecretKeyForJWTGeneration2026!

# ================= EUREKA CONFIG =================
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# ================= LOGGING =================
logging:
  level:
    root: INFO
    com.apigateway.security: DEBUG
    org.springframework.cloud.gateway: INFO

# ================= ACTUATOR =================
management:
  endpoints:
    web:
      exposure:
        include: health,info
